CREATE TABLE books (
    bookID VARCHAR(10) PRIMARY KEY,
    title VARCHAR(255),
    author VARCHAR(255),
    publisher VARCHAR(255),
    year INT,
    isbn VARCHAR(20),
    copies INT
);


